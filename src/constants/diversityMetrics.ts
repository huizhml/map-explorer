/** Must divide `MAX_HEIGHT` (50 m) used by the backend for clean histogram bins. */
export const DIVERSITY_HEIGHT_BIN_OPTIONS = [1, 2, 5, 10] as const;

export const DEFAULT_DIVERSITY_HEIGHT_BIN_M = 5;
